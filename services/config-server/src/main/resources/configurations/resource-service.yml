server:
  port: 3030
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    #    url: jdbc:postgresql://localhost:1002/postgres
    url: jdbc:postgresql://localhost:1000/resource
    username: qwe
    password: qwe
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  liquibase:
    change-log: classpath:/db/changelog/master.yml