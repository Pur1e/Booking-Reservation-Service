server:
  port: 4040
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    #    url: jdbc:postgresql://localhost:1001/postgres
    url: jdbc:postgresql://localhost:1000/user
    username: qwe
    password: qwe
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  liquibase:
    change-log: classpath:/db/changelog/master.yml